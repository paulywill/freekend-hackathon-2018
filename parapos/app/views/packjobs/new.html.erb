<h1>New Packjob</h1>


<%= form_with scope: :packjob, url: packjobs_path,  local: true do |form| %>

<% if @packjob.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@packjob.errors.count, "error") %> prohibited
      this packjob from being saved:
    </h2>
    <ul>
      <% @packjob.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

  <p>
    <%= form.label :"Packer's Name:" %><br>
    <%= form.text_field :packer %>
  </p>

  <p>  
    <%= form.label :"Rig:" %><br>
    <%= form.text_field :rig %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to 'Back', packjobs_path %>
