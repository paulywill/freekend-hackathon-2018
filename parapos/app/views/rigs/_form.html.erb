

<%= form_with model: @packjob, local: true do |form| %>

  <% if @rig.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@rig.errors.count, "error") %> prohibited
        this packjob from being saved:
      </h2>
      <ul>
        <% @rig.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rig_status %><br />
    <%= f.check_box :rig_status %>
  </div>
  <div class="field">
    <%= f.label :rig_type_number %><br />
    <%= f.text_field :rig_type_number %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
